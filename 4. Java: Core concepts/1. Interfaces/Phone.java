package Interfaces;

public interface Phone {
    // In interface, we will only have the declaration and not definition.
    // We can skip the access specifier.
    // Adding all the methods
    String processor();

    String OS();

    int spaceInGB();
}
